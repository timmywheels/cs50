#include <cs50.h>
#include <math.h>
#include <stdio.h>

int main(void)
{

        // float q = 0.25;
    // float d = 0.10;
    // float n = 0.05;
    // float p = 0.01;

    // Get user input
    float change;

    do
    {
        printf("O hai! How much change is owed?");
        change = get_float();

    }while(change < 0);

        int coin_count = 0;

        if()

        coin_count++;
        printf("%.2f", change);
        printf("coin_count:%i", coin_count);

}